# NL Job Scraper — Python dependencies
# Install with: pip install -r job_scraper/requirements.txt

# HTTP
httpx>=0.27.0

# HTML parsing (LinkedIn guest API returns HTML fragments)
beautifulsoup4>=4.12.0
lxml>=5.0

# Environment variables
python-dotenv>=1.0.1

# HTML → Markdown conversion for job descriptions
markdownify>=0.13.1

# Full-text extraction from HTML (Adzuna description enrichment)
trafilatura>=1.12.0

# Testing
pytest>=8.0
