# NL Job Scraper — Python dependencies
# Install with: pip install -r job_scraper/requirements.txt

# HTTP
httpx>=0.27.0

# HTML parsing (LinkedIn guest API returns HTML fragments)
beautifulsoup4>=4.12.0
lxml>=5.0

# Environment variables
python-dotenv>=1.0.1

# hiring.cafe — Cloudflare bypass (launches a stealth Chromium browser to get cf_clearance)
# Also run: playwright install chromium
cf-clearance>=0.31.0
playwright>=1.58.0
