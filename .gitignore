# Dependencies
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Compiled documents
*.pdf
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.xdv

# Environment / secrets (never commit these)
.env
**/.env

# Personal data (never commit these)
job_scraper/state.db
job_scraper/state.db.bak_*
job_scraper/*.md
*_BehavioralReport.pdf
linkedin_Profile.pdf
_backups/

# --- Filled personal application data ---
# Only the *.example template seeds are tracked. The real filled files below are
# personal and must never be committed. Copy a seed, fill it locally, keep it here.
/.claude/skills/job-application-assistant/01-candidate-profile.md
/job_search_tracker.csv
/applications/
# Personal CV / cover-letter LaTeX — only the templates are tracked
cv/*.tex
!cv/main_example.tex
cover_letters/cover_*.tex
# Personal CV-variant index — seed is cv/templates.example.md
/cv/templates.md
# Scratch job lists
jobs_list.txt
_job_batch_*.txt

# Personal photos and signatures
*.jpg
*.jpeg
*.png
!cover_letters/OpenFonts/fonts/**

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Memory files (Claude Code user-specific)
.claude/projects/

# Skills lock (auto-generated)
skills-lock.json
